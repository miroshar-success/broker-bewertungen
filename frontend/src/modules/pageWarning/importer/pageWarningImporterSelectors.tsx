import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const pageWarningImporterSelectors = importerSelectors(
  'pageWarning.importer',
);

export default pageWarningImporterSelectors;
