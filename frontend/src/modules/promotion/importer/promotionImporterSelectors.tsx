import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const promotionImporterSelectors = importerSelectors(
  'promotion.importer',
);

export default promotionImporterSelectors;
