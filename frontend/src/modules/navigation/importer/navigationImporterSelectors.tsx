import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const navigationImporterSelectors = importerSelectors(
  'navigation.importer',
);

export default navigationImporterSelectors;
