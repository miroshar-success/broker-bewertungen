import { styled } from '@mui/material/styles';

const ToolbarWrapper = styled('div')({
  display: 'flex',
  alignItems: 'center',
  flexWrap: 'wrap',
  gap: '8px',
});

export default ToolbarWrapper;
